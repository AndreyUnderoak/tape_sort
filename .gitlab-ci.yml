image: registry.gitlab.com/andreyunderoak/tape_sort:latest

stages:
  - test
  - push   

before_script:
  - chmod +x -R scripts    

test:
  stage: test                 
  script:
    - sh scripts/build.sh
    - sh scripts/test.sh                
